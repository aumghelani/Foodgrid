/** City-wide aggregate statistics displayed in the Government Mode header bar.
 *  Used as placeholderData in useCityStats() while the live API responds. */
export const cityStats = {
  equityScore: 0.61,
  transitCoverage: 0.73,
  highRiskTracts: 4,
  totalTracts: 10,
}
